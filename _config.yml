# Name of your site (displayed in the header)
name: rjbs forgot what he was saying
title: rjbs forgot what he was saying

# Short bio or description (displayed in the header)
description: blathering blatherskite

# URL of your avatar or profile pic (you could use your GitHub profile pic)
avatar: https://avatars.githubusercontent.com/u/30682?v=4

### Flags below are optional

# Includes an icon in the footer for each username you enter
footer-links:
  email:
  flickr: rjbs
  github: rjbs
  instagram:
  linkedin:
  pinterest:
  rss: rjbs # just type anything here for a working RSS icon
  twitter: rjbs
  stackoverflow: # your stackoverflow profile, e.g. "users/50476/bart-kiers"

  # below is correct but pointless: -- rjbs, 2022-02-27
  # youtube: channel/UC80JPlphpCMlDRjvxcHVYZg

twiter:
  username: rjbs
  card: summary

# Enter your Disqus shortname (not your username) to enable commenting on posts
# You can find your shortname on the Settings page of your Disqus account
disqus:

# Enter your Google Analytics web tracking code (e.g. UA-2110908-2) to activate
# tracking
google_analytics:

# Your website URL (e.g. http://barryclark.github.io or
# http://www.barryclark.co) Used for Sitemap.xml and your RSS feed
url: https://blog.rjbs.cloud/

# If you're hosting your site at a Project repository on GitHub pages
# (http://yourusername.github.io/repository-name) and NOT your User repository
# (http://yourusername.github.io) then add in the baseurl here, like this:
# "/repository-name"
baseurl: ""

#
# !! You don't need to change any of the configuration flags below !!
#

permalink: /:year/:month/:title/

# The release of Jekyll Now that you're using
version: v1.2.0

# Jekyll 3 now only supports Kramdown for Markdown
kramdown:
  # Use GitHub flavored markdown, including triple backtick fenced code blocks
  input: GFM
  # Jekyll 3 and GitHub Pages now only support rouge for syntax highlighting
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    # Use existing pygments syntax highlighting css
    css_class: 'highlight'

# Set the Sass partials directory, as we're using @imports
sass:
  style: :expanded # You might prefer to minify using :compressed

# Use the following plug-ins
plugins:
  - jekyll-sitemap  # Create a sitemap using the official Jekyll sitemap gem
  - jekyll-feed     # Create an Atom feed using the official Jekyll feed gem
  - jekyll-paginate # Provide paginator (not 1,000 entries on a page!)
  - jekyll-seo-tag  # Provide opengraph tags and more

paginate: 10
paginate_path: "/blog/page:num/"

# Exclude these files from your production _site
exclude:
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md
  - CNAME
